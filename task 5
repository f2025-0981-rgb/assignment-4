#include <iostream>
#include <vector>
using namespace std;

int main() {
    int m, n;
    cin >> m >> n;

    vector<vector<int>> a(m, vector<int>(n));
    for (int i = 0; i < m; i++)
        for (int j = 0; j < n; j++)
            cin >> a[i][j];

    vector<vector<int>> result(n, vector<int>(m));

    for (int i = 0; i < m; i++)
        for (int j = 0; j < n; j++)
            result[n - 1 - j][i] = a[i][j];

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++)
            cout << result[i][j] << " ";
        cout << endl;
    }

    return 0;
}
